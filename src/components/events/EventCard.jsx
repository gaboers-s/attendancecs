import { CalendarDays, MapPin, Pencil, UserRound, Trash2 } from 'lucide-react'

const dateFormat = new Intl.DateTimeFormat(undefined, { month: 'long', day: 'numeric', year: 'numeric' })

export default function EventCard({ event, status, canManage, onEdit, onDelete }) {
	return <article className="group border-b border-slate-100 p-5 last:border-0 sm:p-6"><div className="flex gap-4"><div className={`mt-1 h-10 w-1 shrink-0 rounded-full ${status === 'current' ? 'bg-emerald-500' : status === 'upcoming' ? 'bg-blue-500' : 'bg-slate-300'}`} /><div className="min-w-0 flex-1"><div className="flex flex-wrap items-start justify-between gap-3"><div><h3 className="font-semibold text-slate-950">{event.title}</h3><p className="mt-1 text-sm leading-6 text-slate-500">{event.description || 'No description provided.'}</p></div>{canManage && <div className="flex gap-1 opacity-100 sm:opacity-0 sm:transition-opacity sm:group-hover:opacity-100"><button type="button" title="Edit event" onClick={() => onEdit(event)} className="rounded-lg p-2 text-slate-500 hover:bg-blue-50 hover:text-blue-600"><Pencil size={16} /></button><button type="button" title="Delete event" onClick={() => onDelete(event)} className="rounded-lg p-2 text-slate-500 hover:bg-red-50 hover:text-red-600"><Trash2 size={16} /></button></div>}</div><div className="mt-4 flex flex-wrap gap-x-5 gap-y-2 text-xs font-medium text-slate-500"><span className="flex items-center gap-1.5"><CalendarDays size={14} className="text-blue-500" />{dateFormat.format(new Date(`${event.event_date}T00:00:00`))}</span><span className="flex items-center gap-1.5"><MapPin size={14} className="text-amber-500" />{event.location}</span><span className="flex items-center gap-1.5"><UserRound size={14} className="text-emerald-500" />{event.organizers}</span></div></div></div></article>
}
